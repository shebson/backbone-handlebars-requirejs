define([
    'router'
    ],
    function (Router) {
        "use strict";
        var init = function () {
            Router.init();
        };
        return {
            init: init
        };
    });